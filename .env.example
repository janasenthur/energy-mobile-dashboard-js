# Database Connection Configuration
# Copy this file to .env and update with your cloud PostgreSQL details

# =====================================================
# ACTIVE CLOUD POSTGRESQL CONNECTION SETTINGS
# =====================================================

# Azure PostgreSQL Database (Ready to use!)
DATABASE_URL=postgresql://nbsadmin:Nb$ad2567!@nbsenergydevdb.postgres.database.azure.com:5432/nbsenergydev_db

# Alternative format (if you prefer individual settings):
DB_HOST=nbsenergydevdb.postgres.database.azure.com
DB_PORT=5432
DB_NAME=nbsenergydev_db
DB_USER=nbsadmin
DB_PASSWORD=Nb$ad2567!
DB_SSL=true

# =====================================================
# BACKUP/ALTERNATIVE CONNECTION SETTINGS
# =====================================================

# Use these if you want to use a different cloud provider
# DATABASE_URL=postgresql://username:password@hostname:port/database_name

# =====================================================
# EXAMPLE CONFIGURATIONS FOR POPULAR PROVIDERS
# =====================================================

# --- SUPABASE (Free tier available) ---
# DATABASE_URL=postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres

# --- NEON (Free tier available) ---
# DATABASE_URL=postgresql://[user]:[password]@[hostname]/[dbname]?sslmode=require

# --- HEROKU POSTGRES ---
# DATABASE_URL=postgres://username:password@hostname:port/database_name

# --- AIVEN (Free tier available) ---
# DATABASE_URL=postgresql://username:password@hostname:port/database_name?sslmode=require

# --- ELEPHANT SQL (Free tier available) ---
# DATABASE_URL=postgresql://username:password@hostname:port/database_name

# --- GOOGLE CLOUD SQL ---
# DATABASE_URL=postgresql://username:password@public-ip:5432/database_name

# --- AWS RDS ---
# DATABASE_URL=postgresql://username:password@rds-endpoint:5432/database_name

# =====================================================
# APP CONFIGURATION
# =====================================================

# API Base URL (for when you build your backend)
API_BASE_URL=http://localhost:3000/api

# Google Maps API Key (for GPS features)
GOOGLE_MAPS_API_KEY=your_google_maps_api_key

# Firebase Config (for push notifications)
FIREBASE_API_KEY=your_firebase_api_key
FIREBASE_PROJECT_ID=your_firebase_project_id

# =====================================================
# SETUP INSTRUCTIONS
# =====================================================

# 1. Choose a cloud PostgreSQL provider (see examples above)
# 2. Create a database account and get connection details
# 3. Copy this file to .env in your project root
# 4. Update the DATABASE_URL with your actual connection details
# 5. Run: database\setup-cloud.bat
